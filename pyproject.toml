[project]
authors = [{name = "will-s-hart", email = "william.hart@maths.ox.ac.uk"}]
dependencies = []
name = "antibody-duration-project"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]

[tool.pixi.pypi-dependencies]
antibody_duration_project = { path = ".", editable = true }

[tool.pixi.tasks]
